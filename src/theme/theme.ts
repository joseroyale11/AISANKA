import {
  COLORS,
  SPACING,
  SIZES,
  RADIUS,
  SHADOW,
} from '../constants';

const theme = {
  colors: COLORS,
  spacing: SPACING,
  sizes: SIZES,
  radius: RADIUS,
  shadow: SHADOW,
};

export default theme;